const ProductDescription = ({ children }: { children: React.ReactNode }) => {
    return (
        <div className="text-center text-sm w-[90%]">{children}</div>
    );
};


export default ProductDescription;